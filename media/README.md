Local TV reels and photo plates for SoengOS.

`*.mp4` — short loopable desks so the theater app plays same-origin
video without remote CORS / bot walls. YouTube remains available
through the Tune YT field, and as a fallback if a local reel fails.

`photos/*.svg` — Song-quiet plates used by the Photos app.
